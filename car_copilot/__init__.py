"""Car Ownership Copilot — a LangGraph multi-agent system.

A supervisor routes each user request to a specialist agent (insurance,
maintenance, recall, cost) that uses mock "internal API" tools to answer.
Mirrors the way Jerry.ai automates inbound sales & service requests.
"""

__version__ = "0.1.0"
